#!/usr/bin/env bash

alias dev='cd ${HOME}/code/src/github.com/ViBiOh/'

EDITOR=vim
TERM=xterm-256color

function update_deps() {
  if [[ -e "package.json" ]]; then
    ncu -u
    npm install --ignore-scripts
  fi

  if [[ -e "Gopkg.toml" ]]; then
    dep ensure -update
  fi
}
