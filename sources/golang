#!/usr/bin/env bash

if command -v go > /dev/null 2>&1; then
  export GOPATH="~/code"
  export PATH="${GOPATH}/bin:${PATH}"

  alias gocode_update="go install ./vendor/..."
  alias pprof_memory="pprof -http=:8080 -sample_index=alloc_space"
fi
