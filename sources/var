#!/usr/bin/env bash

# $1 : Variable name
# $2 : Label for prompt
# $3 : Default value
read_variable_if_required() {
  if [[ -z "${!1:-}" ]]; then
    read -r -p "${2:-${1}}${3:+ [${3}]}=" READ_VALUE
    eval "${1}=${READ_VALUE:-${3}}"
  else
    echo "${1}=${!1}"
  fi
}
