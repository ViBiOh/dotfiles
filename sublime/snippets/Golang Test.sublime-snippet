<snippet>
    <content><![CDATA[
func Test${1:FuncName}(t *testing.T) {
  var cases = []struct {
    input string
    want  bool
  }{
    {
    `test`,
    true,
    },
  }

  for _, testCase := range cases {
    if result := ${1:FuncName}(testCase.input); result != testCase.want {
      t.Errorf(`${1:FuncName}(%v) = %v, want %v`, testCase.input, result, testCase.want)
    }
  }
}

]]></content>
    <tabTrigger>gt</tabTrigger>
    <scope>source.go -(meta)</scope>
    <description>Golang Test</description>
</snippet>
