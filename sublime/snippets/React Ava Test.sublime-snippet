<snippet>
    <content><![CDATA[
import test from 'ava';
import React from 'react';
import { shallow } from 'enzyme';
import ${1:ComponentName} from './';

function defaultProps() {
  return {};
}

test('should always render as a div', (t) => {
  t.is(shallow(<${1:ComponentName} {...defaultProps()} />).type(), 'div');
});

]]></content>
    <tabTrigger>rat</tabTrigger>
    <scope>source.js -(meta)</scope>
    <description>React Ava Test</description>
</snippet>
