<snippet>
	<content><![CDATA[
func Test${2:FuncName}(t *testing.T) {
	t.Parallel()

	t.Run("success", func(t *testing.T) {
		t.Parallel()

		expected := true

		if got := ${2:FuncName}(); got != expected {
			t.Errorf("${2:FuncName}() = %t, want %t", got, expected)
		}
	})
}

]]></content>
	<tabTrigger>gst</tabTrigger>
	<scope>source.go</scope>
	<description>Golang Simple Test</description>
</snippet>
